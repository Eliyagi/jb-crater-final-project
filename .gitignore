# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
.mypy_cache/
.ruff_cache/
# Spark checkpoints and data
spark-code/checkpoints/
spark-code/data_lake/events/
spark-code/jars/
# Local config
.env

# Scratch data (the vendor's downloaded gzipped JSONL lives in a docker volume,
# not here — but students may stash exploratory JSON/parquet under data/).
data/*
!data/.gitkeep
